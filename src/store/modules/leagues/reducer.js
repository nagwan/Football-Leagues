
const INITIAL_STATE = {
    leagues: [],
    league: {}
}

export default function reducer(state = INITIAL_STATE, action) {
    switch (action.type) {
        default:
            return state;
    }
}